<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mysportscenter.dao.TeamVideoDao">
    <select id="getTeamVideoByTeamId" parameterType="String" resultType="com.mysportscenter.javabeans.TeamVideo">
        SELECT * FROM TEAMVIDEO WHERE ID = ''  ORDER BY UPDATE_TIME DESC
    </select>
</mapper>